"""Relay LiveKit agent worker package.

Entry points
------------
- ``relay.agent.trigger.TriggerDetector`` — decides when to fire retrieval.
- ``relay.agent.worker``                  — LiveKit Agents worker process;
  run with ``python -m relay.agent.worker dev`` (or ``start``).
"""
