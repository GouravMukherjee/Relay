{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "tsc -b && vite build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://api.riyanshomelab.com/api/:path*"
    },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
